# 首页 AIGC 实战内容同步设计

## 目标

把首页原有的“AI+编程开发者 / AI 编程实战”统一为“AIGC 实战”，并将首页作品列表中的 02、03 替换为已经加入详情页的两支 AIGC 视频。

## 内容调整

- 首页横向入口卡片：
  - 小标题改为 `01 / AIGC 创作与实践`
  - 主标题改为 `AIGC 实战`
  - 底部引导改为 `查看 AIGC 作品`
- 首页作品区标题改为 `AIGC 实战`
- 详情页主标题改为 `AIGC 实战`，避免首页与详情页名称不一致。
- 首页作品 01 保持不变。
- 首页作品 02 改为：
  - 标题 `AIGC CAMPUS FILM`
  - 说明 `刚出考场就穿越？用 AI 重现中北大学的历史与底蕴`
  - 封面 `assets/ai/aigc-campus-film.jpg`
  - 日期 `2026.9`
- 首页作品 03 改为：
  - 标题 `AIGC EVENT PROMO`
  - 说明 `人说山西好风光——酒BA·汾阳篮球 AIGC 宣传片`
  - 封面 `assets/ai/aigc-event-promo.jpg`
  - 日期 `2026.9`
- 全站年级身份统一为大二：
  - 英文身份标签改为 `IDENTITY: SOPHOMORE_V2.0`
  - 自我介绍改为“中北大学大二学生”
  - 学业状态中的年级改为“大二”

## 点击行为

采用用户选择的方案 1：首页作品 02、03 与 01 一样调用 `openAIPage()`，打开 AIGC 详情页顶部。首页不自动播放视频，也不直接跳转抖音。

## 边界

- 不改变首页横向滑块结构和动画。
- 不改变详情页现有视频播放器、抖音链接和播放方式。
- 不修改 PPT、自媒体或其他板块。

## 验收标准

- 首页入口、首页作品区和详情页标题都显示“AIGC 实战”。
- 页面不再包含“大一”或 `FRESHMAN` 身份文字，并明确显示“大二”身份。
- 首页 02、03 显示对应视频的标题、说明、日期和封面。
- 点击首页 02、03 后，详情页打开且滚动位置位于顶部。
- 桌面和手机视口没有新增横向溢出或 JavaScript 错误。
